import React from "react";
import { Title } from "rbx";

import "rbx/index.css";
import "./Renders.css";

const Renders = () => {
  return (
    <div className="images">
      <Title size={1} textAlign="centered">3D</Title>
    </div>
  );
};

export default Renders;
